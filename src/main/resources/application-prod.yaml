spring:
  jpa:
    properties:
      hibernate:
        format_sql: false
        show_sql: false

management:
  health:
    mail:
      enabled: false

logging:
  level:
    root: info
    org.hibernate: WARN
